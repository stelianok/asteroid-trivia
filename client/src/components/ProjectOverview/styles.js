import styled from "styled-components";

const Title = styled.h2`
  font-size: 2rem;
  font-weight: bolder;
  color: #fff;
`;

const Desription = styled.p`
  font-size: 1.2rem;
  margin: 0;
`;

const FootNote = styled.p`
  font-size: 1.2rem;
  margin: 1rem 0;
`;

export { Title, Desription, FootNote };
